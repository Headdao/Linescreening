# linescreening environment — used ONLY for CI/dev.
# On a real Mac the API key lives in the macOS Keychain (service:
# com.linescreening), never in this file. `.env` is gitignored.
TYPESAFE_API_KEY=
# Optional overrides (SDK defaults shown)
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# TYPESAFE_DEFAULT_MODEL=jev-latest
